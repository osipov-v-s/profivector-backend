# Existing installations predate Flyway history. Baseline them at version 1 so
# the data-preserving B2B transition starts with V2.
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
